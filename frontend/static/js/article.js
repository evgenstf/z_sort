/*
window.onscroll = function() {scrollHandler()};

var header = document.getElementById("z_sort_header");
var sticky = header.offsetTop;

function scrollHandler() {
  if (window.pageYOffset > sticky) {
    header.classList.add("fixed_position");
  } else {
    header.classList.remove("fixed_position");
  }
}
*/

